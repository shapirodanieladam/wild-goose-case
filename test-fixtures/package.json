{
  "name": "test-fixtures",
  "private": true,
  "workspaces": [
    "shared",
    "s1-frontend",
    "s2-api",
    "s3-data"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces"
  }
}